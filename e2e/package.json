{
  "name": "expense-tracker-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "End-to-end tests (Playwright) for the Personal Finance Manager",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
